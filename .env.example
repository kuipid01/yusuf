PAYSTACK_SECRET_KEY=sk_test_abc123...
# PAYSTACK_PUBLIC_KEY is usually used on the frontend if using Paystack Inline
# For this server-side initialization flow, only the secret key is required on the backend
